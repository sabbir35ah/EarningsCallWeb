export const trackStyle = {
  width: "clamp(32px, 6vw, 44px)",
  height: "clamp(18px, 3.5vw, 24px)",
};

export const thumbSize = {
  width: "clamp(14px, 2.5vw, 20px)",
  height: "clamp(14px, 2.5vw, 20px)",
};

export const labelSize = { fontSize: "clamp(10px, 1.8vw, 16px)" };
export const badgeSize = { fontSize: "clamp(9px, 1.3vw, 13px)" };
